# NutriGuard — Gate A dependencies
# Pin exact versions. Update deliberately, not automatically.

# --- AWS SDK ---
boto3==1.38.0
botocore==1.38.0

# --- AgentCore / Strands ---
bedrock-agentcore==0.1.0
strands-agents==0.1.9

# --- Testing ---
pytest==8.3.5
pytest-cov==6.1.0

# --- Type checking and linting ---
mypy==1.15.0
boto3-stubs[ssm,bedrock,bedrock-runtime]==1.38.0
flake8==7.2.0
